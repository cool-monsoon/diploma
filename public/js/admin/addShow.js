document.addEventListener('DOMContentLoaded', function () {
    window.addShow = function () {
        const addShowPopup = document.getElementById('addShowPopup');
        addShowPopup.classList.add('active');
    }
});